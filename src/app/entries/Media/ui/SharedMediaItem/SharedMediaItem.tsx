import React from "react";
import cls from "./SharedMediaItem.module.scss";
type Props = {};

export const SharedMediaItem = (props: Props) => {
  return <div className={cls.mediaItem}></div>;
};

export default SharedMediaItem;
